"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = (arr, predicate) => {
    let p = predicate(arr[0]);
    for (let i = 1; i < arr.length; ++i) {
        const c = predicate(arr[i]);
        if (c < p) {
            return false;
        }
        p = c;
    }
    return true;
};
